<h1>All Clients</h1>
@if (@Model.Count == 0)
{
  <p>No clients have been added yet</p>
}
else
{
  <ul>
    @foreach (var client in Model)
    {
      <li><a href="clients/@client.GetId()">@client.GetClientName</a></li>
    }
  </ul>
}
<a href="/clients/new">Add new Client</a>
